// #[macro_use]
// extern crate criterion;
// extern crate geo;
// extern crate citysearch_api;

// use geo::Point;
// use citysearch_api::scoring::*;

// use criterion::Criterion;

// fn bench_position_score(c: &mut Criterion) {
//     c.bench_function("bench distance", |b| b.iter(|| position_score(20)));
// }

// criterion_group!(benches, bench_position_score);
// criterion_main!(benches);
